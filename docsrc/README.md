To run sphinx correctly, make sure make and python3 (and pip3 and venv) are installed.

Then install sphinx:
```

pip3 install -U sphinx
pip3 install -U sphinxcontrib-phpdomain
pip3 install -U sphinx-rtd-theme
```
